# 核验与交互回归报告

日期：2026-09-13。范围：本轮自定义设置、Agent 状态、灵动岛交互及核验调度。

## 结果摘要

所有结果区分两种来源：受控回答用于检查实现；真实提供方回答用于记录实际体验。受控回答的匹配度不能代表真实渠道模型。

| 方法 | 已执行的验证 | 结果 | 尚未证明的内容 |
| --- | --- | --- | --- |
| HLWY | 内存调用实际评测器 50 次，每次返回公共参考的众数；另验证中断续测 | 50/50 有效；匹配度 82.9059%；自然对数 JSD 0.204906；状态 ok；中断 3 个后补采 47 个；身份结论 inconclusive | 未对真实渠道做本轮 50 次采样；公共参考未标明推理档位 |
| Meow | 实际评测器受控采样，第 4 次中断后继续剩余样本 | 先保存 3 个，再补齐本档计划；失败次数和总尝试数均保留；回归 PASS | 固定测试答案不证明模型方向或识别准确率 |
| 概率探针 | 本地 HTTP/SSE 上游，16 次独立请求，对照固定 ok 分布 | JSD=0；保留 16 次原始调用，分数不合成为 IQ；中断 3 次后续测 13 次通过 | 相同固定答案不能区分模型；实际阈值仍需重复基线实验 |
| Juice | 本地 HTTP/SSE 上游返回 32，校准范围 [32,32] | 原始读数 32；缺基线不请求；503 后原有一致结论失效 | 32 是夹具答案，不是真实服务的推理预算 |
| 可信 API | 此前已授权 `https://api.oaipro.com/v1`，`gpt-5.4-mini`，Responses，low，输出上限 128 | 2/2 请求成功，均为 OK；输入合计 20、输出 33 token；耗时 2671ms / 2853ms；费用未知 | 仅试采样，未建立真实对照分布或身份判断；该地址是用户选定参考，不标作官方 API |

本轮未增加真实付费请求。可信端原始脱敏记录：`.local/verification-live/result.json`。受控请求与真实请求分开保存，没有复制 API Key 到报告或配置。

## 工作中检测策略

- 自适应：已知仍打开的目标允许工作中串行探测，也允许长期待命目标探测。工作中同渠道、同 Key 的请求起始间隔默认 20 秒，可设 5–180 秒。
- 仅空闲：保留近期工作结束后的空闲窗口规则，工作中不请求。
- 只手动：自动调度不启动；手动核验仍可执行。
- 工作结束让行时间默认 3 秒。每条请求实际发出前重新检查当前目标、渠道、Key、模型和推理档位；切换路由或目标消失时停止后续请求。
- 明确启用调度策略的目标遇到请求失败进入同渠道、同 Key 冷却，默认 60 秒，可设 15–900 秒；响应的 Retry-After 更长时采用更长值。
- 每日预算、请求去重和全局串行队列仍保留。普通性能探测默认输出上限 16 token；各核验方法保留自己的采样条件，不通过静默降低推理档位制造不可比结果。

当前限制：低频请求仍会共享提供方的并发和额度，不能保证对主任务没有影响。间隔从请求开始计算；长请求完成后可能立即发下一条。自动检测仍按配置周期轮询，不保证工作结束后立即启动。改变策略对已捕获目标的在途核验生效时机仍需补测。

## 各方法的体验问题与建议

### HLWY

50 次样本在 20 秒工作中间隔下，仅请求间隔就接近 17 分钟，另有网络耗时。公共基准没有推理档位，不能把高匹配度称为同条件认证。中断会保存 `rawNumbers`，下轮只补采缺少的随机数；本次受控续测从 3 个补到 50 个，保留 `continuedFrom` 和累计请求次数。

建议先在空闲时完成整轮，工作中按低频间隔续测；若要将 HLWY 用作长期后台默认方法，仍需对同档位重复测量建立误差范围。

### Meow

低档仍需要多条提示和多次独立采样。当前已支持中断续测和保留失败记录。模型、协议、提示条件不支持时应显示原因，不用固定通用答案充当参考。

建议优先低档，完成一轮后按核验间隔复测；使用候选分布及未知状态，而非只展示单个最相近模型名。

### 概率探针与可信端

至少 16 次是操作下限，不是统计充分性的保证。固定题目只返回 OK 时，不同模型也可能完全一致。试采样 2 次只用于检查连通；正式参考分布的 `maxJsd` 默认为 null，不自动生成真伪阈值。

建议选择有区分力的短答案问题，保存模型、档位、协议、温度、输出上限与时间。用多个同条件批次测得自然波动后再设置阈值。当前受控界面已验证 2 次试采样、16 次正式采集、停止入口及凭据清空，但本轮未进行真实正式参考采集。

### Juice

单次请求成本较低，但返回值属于模型生成内容，不是服务器签名的真实内部参数。提示词、路由处理和模型更新都可能改变结果。

建议只作为额外异常信号，偏离后复测；不把读数当通用 IQ，也不与 JSD 或 HLWY 数值平均。

## UI 与 Agent 验证

已实现：灰色椭圆边界提示、普通形态 1–12 个可见槽位及上下边界滚动、固定三项指标的专注形态、可设置过渡时长、首次 spotlight 引导与重播、主题/颜色/轮廓/交互偏好、Agent 本地 SVG 图标。

可见灵动岛边框随槽位动画变化；原生透明窗口保留至少 420pt 高的详情空间。透明窗口尺寸不等于用户可见边框高度，此选择用于避免悬停时目标跳离鼠标。

Agent 品牌 SVG 固定为 Lobe Icons 1.95.0，缺专属图标使用 Lucide 0.468.0 终端图标。许可证随应用打包。新增状态包括运行、规划、调用工具、等待和错误；明确事件才显示细分状态，仅进程存在不推断为某个具体阶段。

当前真实只读检测已通过 Herdr PID/cwd 关联识别 Claude `idle`。Claude 的本地受控多阶段回归还确认了 `running -> tool -> running -> ended`。

CLI 受控实测：`.ui-artifacts/1789236815227-cli/result.json`，7/7 PASS。Claude Code 使用实际安装的 `/opt/homebrew/bin/claude`，Gemini 0.59.0、Qwen 0.23.3、Pi 0.73.1、OpenCode 1.18.30、Aider 0.86.2、gptme 0.33.0 使用项目隔离安装。每个 CLI 都实际启动、向本地受控上游发请求、收到回答并采集样本；模型、base URL、Key 分组和 cwd 均严格匹配，付费请求为 0。gptme 额外的 `gpt-5-mini` 是自动标题请求，单独记录且不改变主模型识别。Cline/Roo Code 等编辑器扩展不伪装成 CLI，GUI 未下载。

## 证据与待验收

- `npm run check`、`npm run test:core`：通过。
- Runtime：`.ui-artifacts/1789229020780-runtime/result.json`，PASS。
- Web：`.ui-artifacts/1789229507535-web/result.json`，PASS，包含偏好持久化、单槽位边界滚动、自适应目标资格、三形态悬停和空控制台错误。
- 最终增补 Web：`.ui-artifacts/1789229812177-web/result.json`，PASS，包含移动视口、自定义表单不溢出、引导重播/Escape 和上述全部回归。已审阅首次引导、普通/专注、单槽位与移动截图。
- 原生此前：`.ui-artifacts/1789227997361-ui/result.json`，UNTESTED，macOS 会话报告锁屏。
- 解锁后原生：`.ui-artifacts/1789231058952-ui/result.json`，2026-09-13 00:38 完成，56/56 PASS。构建、启动、10 次悬停/离开循环、静止悬停稳定、三指标/详情无裁剪、悬停模型位置稳定、点击详情保留模型选择、设置页、标题栏可拖动且内容区不可拖动、左右吸附与向内展开、菜单退出均通过。
- 已审阅本次 `hover.png`、`island-left.png`、`island-right.png`、`settings-window.png`、`menu.png`。三指标和悬浮详情在窗口内完整显示，左右灰色缓冲条可见。测试应用 PID 6928 及 Node 子进程 PID 6957 已退出，专用 `w1:p8` 已恢复空闲。本次未修改业务代码或发出主动付费核验。
- 原生回归：`.ui-artifacts/1789238534593-ui/result.json`，56/56 PASS。真实启动、10 次悬停展开/收起、三指标和详情无裁剪、标题栏/内容区拖动、左右吸附与向内展开、菜单退出均通过；测试应用和 Node 子进程已退出。普通形态容量上限、上下边界滚动和缓冲条提示仍由 Web 用例覆盖，未列入现有原生专项。
- CLI 多阶段证据：Claude 请求期间 `running`，`PreToolUse` Hook 期间 `tool`，工具结果请求成功，`SessionEnd` 后为 `ended`。进程 cwd 关联与结果文件原子发布修复已在上述回归中生效。
- 后续回归：`.ui-artifacts/1789237401992-web/result.json` 与 `.ui-artifacts/1789237014820-runtime/result.json` 均 PASS；25 个适配器目录、动态高度/边界滚动、自适应工作中检测、可信端表单、校准原始证据和无控制台错误均保持通过。应用包已由 `npm run desktop:build` 重建，`npm run check`、`npm run test:core` 通过。
- 本轮修复：原生灵动岛释放吸附改为完整 frame 动画，并在原生缩放后重新请求 WebKit 布局；原生历史指标与设置分类断言已同步。`npm run ui:test:drag` 曾复现并修复左/右释放吸附，随后因 macOS 再次锁屏被标记 `UNTESTED`，需在保持解锁时重跑确认。
- 最新原生完整回归：`.ui-artifacts/1789245046869-ui/result.json`，95/95 PASS。新增缓冲条点击/拖动、横竖条按住跟随与释放动画中间帧、指标跨窗口导航、四步灵动岛引导和分类设置验收；截图 `native-tour-2.png`、`native-tour-4.png` 已审阅。
- 最新网页回归：`.ui-artifacts/1789245048113-web/result.json`，PASS。最新 Runtime：`.ui-artifacts/1789244814139-runtime/result.json`，PASS，含 HLWY 可信参考 50 次与目标 50 次的受控同条件对照，100 次请求均来自本地夹具，真实付费请求 0。

仍需完成：Cline/Roo/Continue 等 GUI 扩展的实际宿主验证；各方法真实同条件评测。三个扩展已在隔离目录安装，但安装不等于工作流通过。普通形态容量和边界滚动已有 Web 证据；缓冲条动态高度、点击及横竖拖动已有最新原生证据。完整目标保持未完成。
