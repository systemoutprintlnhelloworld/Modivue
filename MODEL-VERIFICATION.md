# 模型质量指标与核验方案

2026-09-13 续作：当前调度已支持自适应工作中低频/故障冷却。最新实测、方法限制及未验收项目见 `VERIFICATION-REPORT-2026-09-13.md`；下文“工作中全部暂停”的说明属于此前版本。

调研日期：2026-09-08。当前产品定义替代此前的“概率探针 + Juice 平均 IQ 分”。

## 决定

三个主要观察维度为：模型核验、Cache、TTFT。模型核验包含定量证据和定性结论，不生成跨模型通用 IQ 分。旧版 35.3 既不是人类智商，也不是 35.3% 的真伪概率；旧公式没有这样的解释依据，不能继续用于显示或告警。历史记录保留，但 v1 分数不进入当前结果。

| 分类 | 指标 | 单位或状态 | 含义 |
| --- | --- | --- | --- |
| 定量 | TTFT | ms / s | 发出请求到首个有效内容；与总耗时分别记录 |
| 定量 | Cache 命中率 | % | 提供方明确报告的缓存读取 token / 输入 token；不是延迟推断 |
| 定量 | Juice 观测 | 原始整数 | 对指定提示词的响应；仅与相同模型、推理档位、模板和输出上限的基线比较 |
| 定量 | 分布 JSD | 0–1，底数为 2 | 多次独立短答案采样与参考分布的距离；0 表示两份分布相同，不代表模型绝对可信 |
| 定量 | HLWY 匹配度 | 0–100% | 上游众数与分布相似公式；不是通过率、真伪概率或 IQ |
| 定性 | 基线比较 | 与基线一致 / 偏离基线 / 证据不足 | 表达已采集证据与参考的关系，不宣称确定路由到另一个模型 |
| 定量 | 样本数、总耗时、估算费用 | 次、ms、USD | 每条性能和核验请求均记录；无价格信息时费用未知 |

Juice 是模型生成的答案，不是提供方认证的服务器运行参数。系统提示词、推理档位、服务更新和响应处理都可能影响它。一次 Juice 偏离只能作为复核信号。主读数展示当前选定方法的结果，并带方法名和单位；没有结果时显示该方法的缺失条件或进度。不同方法不会连成同一条历史曲线。

## 已接入的公共基准

[帖子 2730026](https://linux.do/t/topic/2730026) 对应 [HLWY AI Checker](https://github.com/hanlinwenyuan/hlwy-ai-checker)。应用附带 2026-09-08 下载的三份上游原始基准，并在启动及每六小时读取 GitHub 基准目录更新。断网时保留最近成功下载的数据；这份基准库与 models.dev 标准模型目录各自维护。

当前基准覆盖 `gpt-6-astra`、`gpt-5.6-sol`、`claude-opus-5`。匹配度按 `(众数相似度 + 余弦相似度 × exp(-JS)) / 2 × 100` 计算，JS 使用自然对数，不能混入底数为 2 的自定义概率探针历史。上游未记录 reasoning effort，因此即使匹配度很高，定性结论仍保留证据不足，不称为同档位身份认证。

请求采用上游 1–355 随机整数提示词、温度 1、256 输出 token 上限，并携带当前 Agent 的推理档位。每轮默认 50 次，可设置 50–500 次；至少 40 个有效整数且有效率不低于 80% 才计算匹配度。解析要求整条答案为整数，比上游提取首个整数更严格。每次请求、无效答案、频数、参考分布、来源和版本均保留。

数据、许可证和归属说明位于 `src/data/`，随 macOS 应用打包。Juice v4 可选择“单次原始观测”或“可信校准对照”：前者不需要校准，执行一次请求；后者缺少同条件档案时先提示配置，不发送请求。自定义概率探针 v2 要求明确校准档案。

Meow 使用固定提交 `4108256d0cdc151de3cebec2bd0f5cbbcb2def36` 的 GPT/Claude `4.5.3-predictive.2` 基准和 `predictive.py` 评分移植。既有 56 组 Python/JavaScript 对照通过，最大评分差约 `4e-13` 个百分点；证据为 `.ui-artifacts/meow-4.5.3-parity-20260913.json`。这证明已覆盖场景的评分一致，不能代表完整复刻上游 Web/本地应用。

完整 Meow 档位使用上游计划：GPT 36/72/108 次、Claude 60/90/120 次。Modivue 增加的 6 次快速筛查没有校准后的强指向线。完整采样但未过阈值时显示“采样已完成 · 未超过强指向阈值”，与采样未满额区分。候选特征命中数统计每题最高拟合参考类别，并列类别均计入；它是报告解释项，原评分仍使用完整回答分布。候选共享特征时命中数可以相同。

2026-09-13 通过 `node "scripts/agent-tool.mjs" smart-search fetch URL --format markdown --output FILE` 读取指定资料：[Meow 发布帖](https://linux.do/t/topic/2860498) 可读，确认项目仓库和网页版入口；[Juice 2555348](https://linux.do/t/topic/2555348) 与 [Juice 24576294](https://linux.do/t/topic/24576294) 均返回“页面不存在或私有”。正文快照位于 `.ui-artifacts/meow-2860498-20260913.md`、`.ui-artifacts/juice-2555348-20260913.md`、`.ui-artifacts/juice-24576294-20260913.md`，两条不可读资料保持待补，不能据此声称实现了帖内方法。

## 参考项目

Stars 为本次 GitHub API 快照，只衡量关注度。

| 项目 | 关注度 | 实际用途 | 对本项目的取舍 |
| --- | --- | --- | --- |
| [AxonHub](https://github.com/looplj/axonhub) | 5,179 | AI 网关、协议转换、路由、调用追踪、成本统计 | 参考调用链和费用的可追溯性；本次在默认 unstable 分支文档及递归文件树中未确认独立模型真伪核验模块，不据此声称它不存在于其他版本 |
| [Promptfoo](https://github.com/promptfoo/promptfoo) | 24,925 | 提示词与应用评测、模型对照、红队检测 | 适合另列任务通过率；它不是通用模型身份认证器。当前不加入第三种检测方法 |
| [lm-evaluation-harness](https://github.com/EleutherAI/lm-evaluation-harness) | 13,929 | 标准学术任务与可重复能力评测 | 能给出特定任务指标；成本、样本和评分协议均不同，不平均成“模型 IQ”，当前不接入常驻轮询 |
| [LLMmap](https://github.com/pasquini-dario/LLMmap) | 447 | 黑盒模型指纹识别 | 参考身份判定与能力测量的区别；训练覆盖有限，不用于给未知新模型硬贴标签 |
| [llm-fingerprint](https://github.com/dreamor/llm-fingerprint) | 3 | 多次短答案分布、JSD、参考库匹配 | 与概率探针需求吻合；并非高星成熟性证明。参考其方法，不能照搬 README 的通用阈值或准确率到当前渠道 |
| [Any-Api-Check](https://github.com/xiaopenghuang/Any-Api-Check) | 47 | 中转余额与日志、连通性、Claude 真伪性检测 | README 明确列出核验功能；与 AxonHub 区分。没有把其启发式结论当成身份认证 |

原始需求参考：[概率探针讨论](https://linux.do/t/topic/2726624)、[Juice 讨论](https://linux.do/t/topic/2704354)。研究工具：Smart Search fetch 成功，主搜索因配置模型不支持返回 404；项目发现明确改用网页搜索，源代码与 Stars 用 GitHub 公共 API 读取。

## 判定边界

1. 提示词、参考分布、推理档位、采样温度、输出 token 上限必须随校准档案保存。只比较相同条件。
2. 不再内置三道有标准答案的题。概率探针从校准档案加载提示词，多次新请求建立实测分布，保存原始频数与各题 JSD。
3. 每题允许 16–100 次采样。16 次只是当前操作下限，不是统计充分性的保证。阈值必须由同条件重复基线实验确定；未提供 `maxJsd` 时只显示距离，结论为证据不足。
4. 自定义概率探针对照目标模型参考分布。Meow、HLWY 和有同条件候选范围的 Juice 分别展示各自候选证据；候选覆盖范围与拒绝条件按方法说明，未知或相近候选不强行判定。
5. Juice 单独输出原始值及参考闭区间，不缩放到 0–100，不与 JSD 相加。
6. 主状态使用当前选定方法的最新记录，不合并不同方法结论。请求失败、采样中断、校准缺失及未过阈值分别显示原因；保留上一有效数值时标明它的时间和最新未完成状态。
7. 连续偏离告警按渠道、Key、模型、方法、校准修订和推理档位分组。失败和不确定结果中断连续偏离计数。

## 运行与成本

默认启动主动检测，启动时排期一次，后续按设置间隔执行。只使用检测到的运行中顶层 Agent；同一目标去重，子 Agent 不独立探测。只存在配置文件不构成运行证据。无法对应当前渠道的凭据时不借用其他渠道凭据。

每轮执行性能请求及当前选定的核验方法。性能请求暂时失败不会跳过核验；鉴权失败、目标不再符合当前调度策略、预算耗尽会停止。自定义概率探针和校准模式 Juice 缺少校准时不发请求；原始模式 Juice 只发一次请求；HLWY 使用公共基准或可信 API 基准。全部实际请求共享每日请求上限，达到上限后停止请求。分布采样开始前检查整轮请求额度，单次发出前再次检查。关闭主动检测时，正在传输的一条请求可能仍完成；后续请求停止。

核验费用按实际请求数计算。每轮只运行当前选定方法；例如 HLWY 设置为 50 次时，计划为 50 次核验加 1 次性能探测，重试另计。新安装每日请求上限为 256，现有用户设置保留。1 分钟间隔并不保证一分钟内完成整轮，当前采用上一轮结束后再等待间隔，防止重叠堆积。

缓存率反映这组检测提示词的缓存行为，不代表整个 Coding Agent 工作负载。真实 Agent 调用经过本地代理时另记为被动观测，不混淆采样来源。

## 校准格式 v2

校准文件只支持 Modivue 明确格式，不声称兼容任一上游工具导出文件。`models` 的键使用标准模型 ID 或精确的观测名称。每个记录包含：

- `revision`：校准版本标识；修改条件或参考数据应使用新版本。
- `reasoningEffort`：明确档位字符串，或 `null` 表示请求未指定。
- `maxOutputTokens`：与参考实验一致的输出上限。
- `probability`：`cells: [{prompt, distribution}]`、`repetitions`、`temperature`、`maxJsd`；温度未发送时为 `null`，阈值未校准时为 `null`。
- `juice`：`prompt`、`min`、`max`，保留该模型对应的原始量纲。
- 顶层 `source`：参考实验或数据集来源，`version: 2`。

现有 HLWY 公共基准覆盖 `gpt-6-astra`，但不能据此声称已获得 `/ high` 同档位基线。Meow 已接入公开 benchmark、低档采样和中断续测，其模板、采样档位与 HLWY 不同，不混合评分。AxonHub 所称三种核验方法仍待确认具体源码位置；没有以推测实现替代它们。

## 可信 API 回答对照（2026-09-12）

设置页“可信 API 回答对照”直接采集用户指定渠道的回答，复用概率探针 v2。选择模型、协议、推理档位、温度、输出上限和短答案提示词。试采样固定 2 次，检查连通和响应，不写入校准；正式采样 16–100 次保存频数归一化分布。Key 只在本轮内存中使用，不返回给浏览器状态、不写校准。采样进度支持查询与停止；当前在途请求可能完成。

档案保留提供方 source、协议、采样数量、时间与修订。正式采集替换同模型记录、保留其他模型。目标核验使用相同提示词和请求条件，API 协议或推理档位不一致时停止并提示。未发送温度只能证明请求条件相同，不能证明两个提供方的默认温度一致。

自动生成的 maxJsd 为 null：只报告 JSD、频数和样本数，不产生未经实验校准的身份阈值。固定问题得到相同答案不证明模型相同；样本少或候选模型回答相近也不能认证身份。HLWY 公共分布与可信端自采分布分别展示。

已授权真实流程验证：2026-09-12，`https://api.oaipro.com/v1`，`gpt-5.4-mini`，Responses，low，maxOutputTokens=128，提示“只回复 OK。”；2/2 成功，均 OK，输入20、输出33 token。费用未提供，记录未知。该渠道是用户指定可信参考，不标作官方；未用两次答案建立身份基线。脱敏证据：`.local/verification-live/result.json`。

手动核验 API 立即返回202。工作会话可以排队；工作中不发主动请求，短 idle 窗口开始执行。主面板、悬浮详情和核验页显示排队原因、重试、有效样本和耗时。队列内存保存；历史请求/报告仍由 SQLite 持久化。
