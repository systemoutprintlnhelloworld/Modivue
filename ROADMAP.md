# Modivue 路线图

更新时间：2026-09-15

## 2026-09-15 原生 hover 修复

- 修复激活灵动岛时误触发主窗口重开的问题；同步原生引导断言为实际 7 步。
- `.ui-artifacts/1789405112503-ui/result.json` 全部 PASS，原生 hover 失败项已复验通过。

## 2026-09-15 Codex Finder 启动修复

- 根因：默认 Herdr 会话协议不匹配，Finder 缺命名会话 socket，退回进程记录而显示 5 个。
- 已改为发现各本地会话，并按所属 socket 查询 pane PID。精简环境原生启动返回 3 个（finder-clean-oW42lk），JS/core/build/signature 通过；旧实例需完全退出重开新包。
- 原生鼠标回归 1789484754296-ui 因重新锁屏 UNTESTED；用户原应用未被关闭。

## 2026-09-15 六项反馈当前状态

- 已改：专注/标准界面横向复选栏、四环高度及余额单选；Codex pane 对照去重；文件选择 sheet 回调和独立 Edit 菜单；HLWY 默认 10 次、允许 1–500 次且小样本仅预览；玻璃渐变/辉光/原生材质。
- 已验：Web 48 PASS（1789483419966-web），临时交互验证目录 feedback-sf3O1z，JS/i18n/core/Swift、最终 macOS 构建和签名通过。Codex 现场 pB working、p2/pQ idle 与 API 一致；HLWY 受控 1/10/50 次，付费模型调用 0。
- 待验：原生文件导入是否不再闪退、Cmd+C/V、四环 hover 及真实桌面玻璃材质。1789483679289-ui 因锁屏 UNTESTED；历史原生 PASS 不能代替本次验收。
- 最新包 dist/Modivue.app；未提交或发布。完整记录以 HANDOFF 顶部为准。

## 2026-09-15 续作记录

- 已完成可信补充队列的失败隔离、退避重试、取消/鉴权/预算错误续采；尚缺受控 HTTP 对失败后继续及取消的独立证据，checkpoint 也未持久化到重启后。
- 已完成非 Juice 跨推理强度质量共享、专注/标准形态横向环复选栏、余额单环、校准档案导出和全方法分布档案导出；公共基准导入请求体上限已与前端 5 MiB 一致。
- 本轮 `npm run ui:test:web`、`npm run runtime:test`、`npm run check`、`npm run check:js`、`npm run check:i18n`、`npm run test:core`、`npm run ui:test` 均通过；原生 hover 历史 FAIL 已修复并由 `.ui-artifacts/1789460584410-ui/result.json` 全部 PASS 覆盖。
- 最新证据：`.ui-artifacts/1789402996060-web/result.json`、`.ui-artifacts/1789403829782-runtime/result.json`。

更新时间：2026-09-14

## 当前本地续作

最新交接与证据以 [HANDOFF.md](HANDOFF.md) 顶部为准。已发布版本仍为 v0.4.1，本地 `47e5375` 之后有未提交改动；本地包版本号同为 0.4.1，代码与发布标签不同。本次仅维护交接文件。

- 已实现：Codex 顶层会话去重、非工作目标仅手动核验与暂停/终止/插队、余额 15 秒刷新及充值基准、余额环/核验环二选一、API 实际费用 > 渠道单价估算 > models.dev 单价估算。
- 已实现：Meow/Astra/One Token 跟随四元组推理档位、Meow 六题预览参考线、可用性排序的核验方法卡片下拉、补充样本入口、按方法/单题筛选历史、动态详情高度、毛玻璃与穿透模式。
- 水杯题回答收集已取得真实渠道成功回答，并通过受控 46 秒 HTTP 回归；保留不限时请求、完整原文及答案/证明解析。证明正确性仍人工复核，不能把与参考答案不一致当作收集失败。
- 官方 Ztest 浏览器提交/轮询与本地五组兼容探针分别实现；官方端到端报告仍待验证。BazaarLink 定性结论与已有定量字段使用上游报告，不编造综合 IQ。
- 最新 Web 48 PASS、Runtime 27 PASS，原生 `.ui-artifacts/1789405112503-ui/result.json` 全部 PASS；旧失败已定位并修复。

## 接下来按优先级推进

1. 原生 hover、拖动吸附、详情自适应高度和穿透恢复已通过 `.ui-artifacts/1789405112503-ui/result.json`；后续仅在相关代码变更时复验。
2. 验收真实余额下降/充值、应用内告警与系统通知权限/横幅；确认 15 秒刷新及告警重试对实际渠道有效。
3. 验收当前四元组 Meow/HLWY 基准覆盖与推理条件、补充样本入口、历史方法/题目独立选择。缺基准仍显示原因，不能用别的档位得出结论。
4. 完成官方 Ztest 使用真实 Key 的端到端报告；取得 Astra 原始分布、One Token 四语言全集及两篇指定 Juice 正文后精确对照实现。保持未评估/证据不足标记。
5. Windows 实机鼠标、透明窗口、多 DPI、系统通知与 Hook；Cline/Roo Code/Continue GUI 的真实请求链路；更多可信渠道同条件多批校准。
6. 继续用户已提出的 GitHub 同步与发布工作，审核待提交文件并跑相关检查，更新版本后验证 Actions 两平台产物。本地保留最新可运行包；旧包与测试产物清理先核对备份，保护数据库、凭据和未提交源码。

以下为各已发布版本的历史交付范围。

## 已交付 0.2.0 预览版

- macOS 原生主窗口、灵动岛、悬停详情、拖动左右吸附、动态缓冲条。
- Windows .NET 8 + WebView2 宿主骨架；CIM 进程发现和本地数据目录。
- Spotlight 搜索（⌘K/Ctrl+K）、无滚动弹窗、键盘导航。
- 设置分类、设置搜索、自动保存、输入校验、失败保留草稿；中英文界面切换。
- HLWY、Meow、Juice、概率探针、单问题题库及自定义问题。
- Ztest 独立检测适配：官网检测后导入 URL/JSON，四元组归属确认、原始报告持久化和重复导入去重。
- CLI 状态、Agent、报告、持续观察；GitHub Actions macOS/Windows 构建和 v* 预览发布。

## 已交付 0.3.0 预览版

- 官方 BazaarLink 异步检测、持续计划、每目标同意、每日轮数、启动去重、重启读原 runId、停止与报告保存。
- 单题独立 5–3600 秒间隔、默认 60 秒、单次请求，按题目版本／四元组／时间窗口统计；UI 与状态栏共用计算。
- 系统语言默认选择、macOS 和 Windows 原生语言注入；英文设置／引导／状态／报告文案扩充。
- 文字缩放和独立正文颜色；内部 SVG 动画及环高亮；原生主题标题栏和内容布局约束。

## 已实现 0.4.0

- 核验控件/历史稳定、hover SVG、过渡动画、主题强调色、80%–200% 字体。
- 单题独立超时/输出预算、原生文件导出、系统通知与 toast。
- Meow 4.5.4 正式原生/Chat 基准、OpenRouter 参考样本展示、192 案例评分对照。
- KBF 公开历史参考完整检验、One Token 英文适配、Astra 五组观测；后两者支持分布档案导出和同条件导入。
- 英文正文/提示/可访问名称扫描及展开报告覆盖。

## 0.4.1 修订

- 英文翻译扫描覆盖动态无障碍标签、三种方法展开报告、灵动岛三态和语言往返；不再跳过整个灵动岛控件。
- 缺失数值的环标签显示 `--`，完整状态在详情与无障碍名称保留。
- 分布参考档案完成浏览器导出读回验证，目标档位与采样档位分别记录。

## 后续任务

- Windows 实机鼠标、透明窗口、多 DPI、WebView2 Runtime 和代码签名验收。
- Cline、Roo Code、Continue 等 GUI Agent 的真实请求链路验证；补充 Windows Hook/状态事件。
- Ztest 登录态报告读取、更多报告版本兼容；不绕过 Turnstile，不代填用户凭据。
- 英文界面、设置、引导、方法长说明已纳入脚本；提供方返回的原文错误与未知历史数据按原文显示。题目和原始回答按源语言保留。
- Astra 原帖精确提示词/原始分布、One Token 四语言原始题库；更多可信渠道的同条件多批采样。
- 从用户指定的 Juice 资料恢复可读原文后，对照其 Web/本地实现并记录差异。
- 评测方法继续保持“证据与能力分离”：不把外部探针或单题结果合成为 IQ，也不把证据不足写成确定身份。
- 更多方案来源继续保留：[linux.do 2861517](https://linux.do/t/topic/2861517)、[单题来源 2797049](https://linux.do/t/topic/2797049)、[Juice 2555348](https://linux.do/t/topic/2555348)、[Juice 24576294](https://linux.do/t/topic/24576294)。可读方法逐项复现后接入，当前无法读取的两篇 Juice 正文保持待补。

## 发布验证

- v0.4.1 已发布 macOS arm64 与 Windows x64 ZIP，发布流水线 34763066572 成功；最终验收脚本提交 57a40f6 的两平台构建 34764259270 成功。应用代码与发布标签一致。
- v0.4.1 原生完整回归 105 项 PASS（`.ui-artifacts/1789311386669-ui/result.json`）：悬停、精确四元组及指标跳转、原生保存 JSONL、引导、拖动吸附和退出；概览、核验页与专注引导截图已审阅。系统通知横幅与权限仍待实机验收。
- v0.4.1 Web 47 项 PASS，44 个英文场景扫描无遗漏（`.ui-artifacts/1789309444661-web/`）；Runtime 25 项 PASS（`.ui-artifacts/1789306639635-runtime/`）。
- v0.3.0 已自动发布 macOS arm64 与 Windows x64 ZIP，GitHub Actions 34755423096 两平台构建、核心检查与发布通过。
- 本地 Web 40 项通过，覆盖自动保存、Spotlight、语言／字体／颜色、SVG 动画、单题调度与统计、BazaarLink 启动／轮询／停止／预算／报告及表单保留。5010 条完整窗口统计、跨进程恢复和报告导入／停止失败另有隔离验证。全程使用受控上游，新增真实付费请求 0。
- macOS 原生 97 项通过，覆盖启动、主题标题栏／内容区布局、悬停、稳定位置、左右吸附、横竖拖条、跨窗口指标跳转、引导与退出；详情和悬停截图已审阅。
- Windows .NET 8 交叉编译 0 警告、0 错误；GitHub Windows runner 打包通过。尚未替代 Windows 实机交互验收。
