# 模型 IQ 与灵动岛重构

## 2026-09-16 新增需求（覆盖相关旧说明）

- 花费统计按当前 provider 动态展示多张余额趋势图，保留渠道、Key 分组、单位、真实时间与样本数。缺失或单点数据明确显示，不补造历史。
- 模型核验、TTFT、Cache、请求失败、余额等通知类型分别支持启用、静音与音效选择；保留通知去重和既有用户总开关。
- 按 `建议/README.md` 的展示结构美化根 README，依据源码校正文案。用户自行提供真实录屏 GIF 和截图；先提供标明用途的可替换模板与路径说明，不把模板称为实际界面。
- 最新授权：审查并同步非私有源码和过程文档到 GitHub，包括 `建议/`；不打发布标签，不停止旧用户后台进程，不发起付费核验，不开发已有上下文环与定时指标播报 TODO。
- README 综合 `建议/README.md` 与 `建议/readme2.md`：无装饰 emoji，应用图标与名称同行，下载 badge、15 个 Agent 品牌 badge、九宫格图文功能与二级文档链接；每种核验注明真实来源和实现限制。模板不冒充真实截图，未部署文档站不冒充已有站点。
- 复核 CC Switch 切换 provider 后仍走旧渠道的问题，区分配置读取与实际代理转发；不把某个 Agent 的当前 provider 静默用于所有请求。

## 用户已确定

- 对外统一称为模型 IQ。只采用概率探针和 Juice 值，参考 https://linux.do/t/topic/2726624 与 https://linux.do/t/topic/2704354；不加入 coding、知识或其他能力基准。
- 默认检测间隔 1 分钟，可在设置中调整。每次检测记录费用与来源，累计测试花费。
- 主窗口紧贴屏幕左右边缘，可上下拖动，横向拖动结束时吸附最近一侧。
- Hover 扩展从主窗口生长，连接处随指针变化并体现胶状弹性，不做横向滑入箭头。
- 三环中心显示 IQ。各环独立颜色，当前值深色、最大值浅色、最小值标记；Hover 单环放大并在当前弧端显示数值，健康变化使用同色系渐变。
- 扩展和详情显示历史曲线、平均请求总耗时；图表支持交互。
- 导航切换替换右侧内容；改进日间控件、筛选、主题与数值动效。

## 验收证据

- 指标和成本：受控协议响应经实际采集、存储、聚合、GUI；缺失数据不能变成零。
- IQ：上游两种算法、许可、基线和模型适用范围可追溯，综合公式与原始分项可查；真实端点验证按成本上限执行。
- 原生交互：Herdr 宿主鼠标操作、窗口坐标、连续 Hover、两侧吸附、独立环命中和截图。
- 详情：逐个 Tab 只显示对应视图，明暗模式、图表悬停与缩放、设置持久化和费用显示。

## 反馈

- 2026-09-08：寸止调用超时，没有收到反馈，未视为同意。
- 用户随后明确只用概率探针和 Juice，覆盖此前三维能力评测建议。
- 费用实际/估算/未知方案已发出异步问题，等待回复；先实现区分来源的数据契约。

## 2026-09-20 操作杆与中心图标补充

- 顶部横杆和侧面竖杆缩短，保持居中；悬停出现、离开淡出，拖动和尺寸编辑期间保持可见，不改变原生命中区域的可用性。
- 设置可选 silent/normal 环中心使用模型图标或该四元组关联的 Agent 图标；默认模型，同类型 Agent 去重，多类型不任意丢弃。focus 仍显示指标图标。
- 验证设置持久化、两种形态一致、无会话不伪造 Agent、动画与减少动态效果；不新增测试代码、不提交或发布。
